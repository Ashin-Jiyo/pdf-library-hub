# Add these environment variables to your .env file for the guest Firebase project

# Guest Firebase Configuration (from your new Firebase project)
VITE_GUEST_FIREBASE_API_KEY=your_guest_api_key_here
VITE_GUEST_FIREBASE_AUTH_DOMAIN=your_guest_project_id.firebaseapp.com
VITE_GUEST_FIREBASE_PROJECT_ID=your_guest_project_id
VITE_GUEST_FIREBASE_STORAGE_BUCKET=your_guest_project_id.appspot.com
VITE_GUEST_FIREBASE_MESSAGING_SENDER_ID=your_guest_messaging_sender_id
VITE_GUEST_FIREBASE_APP_ID=your_guest_app_id
VITE_GUEST_FIREBASE_MEASUREMENT_ID=your_guest_measurement_id

# Instructions:
# 1. Create a new Firebase project for guest authentication
# 2. Enable Authentication with Email/Password provider
# 3. Get the Firebase config from Project Settings > General > Your apps
# 4. Replace the values above with your actual Firebase config values
# 5. Add these to your existing .env file

# Guest Firebase Setup Steps:
# 1. Go to https://console.firebase.google.com
# 2. Click "Create a project" or "Add project"
# 3. Name it something like "pdf-library-guest" or "pdf-hub-guest"
# 4. Enable Google Analytics (optional)
# 5. Go to Authentication > Sign-in method
# 6. Enable "Email/Password" provider
# 7. Go to Authentication > Users
# 8. Click "Add user" to create test accounts for your guests
# 9. Go to Project Settings > General > Your apps
# 10. Click "Add app" > Web app
# 11. Register your app and copy the config object
# 12. Replace the values above with your config values
